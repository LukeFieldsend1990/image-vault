{
  "name": "image-vault-comms-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --config comms-worker/wrangler.toml",
    "deploy": "wrangler deploy --config comms-worker/wrangler.toml",
    "cf-typegen": "wrangler types --config comms-worker/wrangler.toml --output comms-worker/worker-env.d.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.109.3"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.2"
  }
}
