/**
 * Import all MCP tool definition modules so they self-register.
 * Adding a new tool: create a file in this directory (or add to an existing
 * one) calling registerMcpTool(), then import it here.
 */

import "./semantic";
import "./visibility";
import "./administration";
import "./onboarding";
import "./production-cast";
import "./tmdb-cast";
import "./likeness-monitor";
